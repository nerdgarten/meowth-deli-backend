import { Router } from "express";

const router = Router();

// This route file is a placeholder for user-related endpoints.
// The application currently exposes user data through other routes (e.g., /customer, /driver, /restaurant).
// Export an empty router to avoid build errors until a dedicated user controller is implemented.

// router.get("/:id", getUser); // Intentionally commented out

export default router;
